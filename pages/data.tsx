import React from 'react';

// a page to just list all queens and seasons so we know the data is being created correctly and everything?
export default class Data extends React.Component {
    render() {
        return (
            <div>
                test
            </div>
        );
    }
}
